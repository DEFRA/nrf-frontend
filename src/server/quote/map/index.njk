{% extends "layouts/page.njk" %}

{% block pageTitle %}{{ pageTitle }}{% endblock %}

{% block head %}
  {{ super() }}
  <link href="/public/interactive-map/interactive-map.css" rel="stylesheet">
{% endblock %}

{% block beforeContent %}
  {{ backLink({href: backLinkPath}) }}
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">

        <h1 class="govuk-heading-l">{{ pageHeading }}</h1>

        <p class="govuk-body">
            Your boundary file has been validated successfully.
            {{ featureCount }} feature{{ "s" if featureCount != 1 }} found.
        </p>

        <div id="boundary-map" class="govuk-!-margin-bottom-6"
             data-geojson='{{ boundaryGeojson | safe }}'
             data-map-style-url="{{ mapStyleUrl }}">
            <p class="govuk-body govuk-!-margin-bottom-0">Map view is not available. The boundary data has been validated.</p>
        </div>

        <details class="govuk-details govuk-!-margin-bottom-6">
            <summary class="govuk-details__summary">
                <span class="govuk-details__summary-text">View boundary GeoJSON</span>
            </summary>
            <div class="govuk-details__text">
                <pre>{{ boundaryResponseJson }}</pre>
            </div>
        </details>

        {% if intersectsEdp %}
        <p class="govuk-body">
            Your red line boundary is within {{ intersectingEdps | length }} EDP{{ "s" if intersectingEdps | length != 1 }}:
        </p>
        <ul class="govuk-list govuk-list--bullet">
            {% for edp in intersectingEdps %}
            <li>{{ edp.label }}{% if edp.n2k_site_name %} ({{ edp.n2k_site_name }}){% endif %}</li>
            {% endfor %}
        </ul>

        {{ govukButton({
            text: "Edit",
            classes: "govuk-button--secondary",
            disabled: true
        }) }}

        <form method="post" novalidate>
            {% include "partials/csrf-token.njk" %}

            {{ govukButton({
                text: "Save and continue",
                preventDoubleClick: true
            }) }}
        </form>

        {% else %}

        <h2 class="govuk-heading-m">EDP boundary check</h2>
        {{ govukInsetText({
            text: "There are no EDPs within the red line boundary."
        }) }}

        <form method="post" novalidate>
            {% include "partials/csrf-token.njk" %}

            {{ govukButton({
                text: "Save and continue",
                preventDoubleClick: true
            }) }}
        </form>

        {% endif %}

    </div>
</div>
{% endblock %}

{% block bodyEnd %}
  {{ super() }}
  <script src="/public/interactive-map/core/index.js" nonce="{{ nonce }}"></script>
  <script src="/public/interactive-map/maplibre/index.js" nonce="{{ nonce }}"></script>
  <script src="{{ getAssetPath('boundary-map.js') }}" nonce="{{ nonce }}"></script>
{% endblock %}
