import { wasteWaterGetController } from './controller-get.js'
import { quotePostController } from '../controller-post.js'
import getViewModel from './get-view-model.js'
import formValidation from './form-validation.js'
import getNextPage from './get-next-page.js'

const routeId = 'waste-water'
export const routePath = '/quote/waste-water'

/**
 * @openapi
 * /quote/waste-water:
 *   get:
 *     tags:
 *       - Quote
 *     summary: Waste water treatment works selection page
 *     description: Renders the waste water treatment works selection form
 *     responses:
 *       200:
 *         description: HTML form page
 *         content:
 *           text/html:
 *             schema:
 *               type: string
 *   post:
 *     tags:
 *       - Quote
 *     summary: Submit waste water treatment works selection
 *     requestBody:
 *       required: true
 *       content:
 *         application/x-www-form-urlencoded:
 *           schema:
 *             type: object
 *             required:
 *               - wasteWaterTreatmentWorks
 *             properties:
 *               wasteWaterTreatmentWorks:
 *                 type: string
 *     responses:
 *       303:
 *         description: Redirect to next step on success or back to form on validation failure
 */
export default [
  {
    method: 'GET',
    path: routePath,
    ...wasteWaterGetController({ routeId, getViewModel })
  },
  {
    method: 'POST',
    path: routePath,
    ...quotePostController({
      routeId,
      formValidation,
      getViewModel,
      getNextPage
    })
  }
]
