:3000 {
    # Route upload requests to nrf-backend
    handle /upload-and-scan/* {
        reverse_proxy nrf-backend:3098
    }

    # Route everything else to the node app on host
    handle {
        reverse_proxy host.docker.internal:3001
    }
}
