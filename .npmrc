git-tag-version=false
# The defradigital/node(-development) base images disable git-protocol
# dependency fetches by default (npm's allow-git=none). @defra/nrf-library is
# installed via a github: dependency, so this narrowly re-allows only
# git dependencies declared directly in package.json (not transitive ones).
allow-git=root
